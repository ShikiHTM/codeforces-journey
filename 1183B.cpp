#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define buff ios_base::sync_with_stdio(0); cin.tie(0)

void sol(void) {
	int n ;cin >> n;
	ll k; cin >> k;

	vector<ll> price(n);
	for(auto &it: price) cin >> it;
}

int main() {
	buff;
	int iTest; cin >> iTest;
	while(iTest--) sol();
}

